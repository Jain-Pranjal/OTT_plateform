import React from 'react'

const DetailsOfMovie = (props) => {
  return (
    <div>
      the movie id is {props.id} and the synopsis is {props.synopsis}
    </div>
  )
}

export default DetailsOfMovie
